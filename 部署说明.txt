阿憨植造 · 部署包（由 build-deploy.js 生成，JS 已混淆）
生成时间：2026/9/15 16:39:17

【目录内容】
  index.html      设计器主页面（JS 已混淆）
  admin.html      管理后台（JS 已混淆）
  server.js       服务端（唯一后端）
  启动阿憨植造.bat / 守护阿憨植造.bat / 防火墙放行8080.bat
  data/           未包含，首次启动自动生成空结构

【部署方式】
  1) 安装 Node.js 18+；
  2) 双击「启动阿憨植造.bat」或命令行 node server.js；
  3) 默认端口 8080，局域网访问 http://本机IP:8080；
  4) 素材目录已加登录鉴权：任何人直接访问 /data/ 下图片返回 403，
     必须登录（带有效 token）才能加载素材。

【上微信小程序时的注意】
  - 建议用原生小程序（不要 H5 套 web-view），代码安全更高；
  - 配置合法域名指向本服务；
  - 导出图/效果图建议后续改服务端合成，进一步防素材被抓；
  - 服务端接口（/api/*）已支持 Bearer token，小程序请求头带上即可。

【素材鉴权说明（已内置）】
  /data/materials/ 与 /data/thumbs/ 均校验 ?token= 参数，
  无 token 或 token 无效一律 403。前端已自动拼接 token。